[project]
name = "cookie-cats-causal-inference"
version = "0.1.0"
description = "A/B test analysis with causal inference methods"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
